import * as React from 'react';
import { Text } from '@chakra-ui/react';

const Publications = (): JSX.Element => (
	<>
		<Text variant="h5">TODO: /publications</Text>
	</>
);

export default Publications;
