import * as React from 'react';
import { Text } from '@chakra-ui/react';

const Decouvertes = (): JSX.Element => (
	<>
		<Text variant="h5">TODO: /decouvertes</Text>
	</>
);

export default Decouvertes;
