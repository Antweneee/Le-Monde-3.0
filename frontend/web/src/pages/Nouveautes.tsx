import * as React from 'react';
import { Text } from '@chakra-ui/react';

const Nouveautes = (): JSX.Element => (
	<>
		<Text variant="h5">TODO: /nouveautes</Text>
	</>
);

export default Nouveautes;
