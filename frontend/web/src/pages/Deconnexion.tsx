import * as React from 'react';
import { Text } from '@chakra-ui/react';

const Deconnexion = (): JSX.Element => (
	<>
		<Text variant="h5">TODO: /deconnexion</Text>
	</>
);

export default Deconnexion;
